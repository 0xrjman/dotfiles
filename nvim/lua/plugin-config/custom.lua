-- https://github.com/kyazdani42/nvim-tree.lua
-- local nvim_tree = require'nvim-tree'
-- local status, vim_clap = pcall(require, "vim-clap")
-- if not status then
--  vim.notify("vim-clap not found")
--  return
-- end

-- local status, markdown_preview = pcall(require, "markdown-preview")
-- if not status then
--  vim.notify("markdown-preview not found")
--  return
-- end

-- 列表操作快捷键
-- local list_keys = require("keybindings").vim-clap

